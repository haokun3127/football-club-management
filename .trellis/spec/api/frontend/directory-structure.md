# API Frontend Directory Structure

`apps/api` currently has no browser frontend source. Do not invent component, hook, or state-management patterns for this package.

If frontend code is later added under `apps/api`, update this file from real code examples before using this layer in Trellis tasks.
