# P1 周日历展开月历改版 — 实施计划

1. Read current online P1 frames, tokens and components; create the three revised Figma states without altering historical frames.
2. Capture Figma screenshots and record the new node identifiers in the task context.
3. Read the mini-program specifications and current P1 code. Add targeted failing/updated tests for collapsed/expanded date-picker behavior.
4. Implement the smallest TypeScript/WXML/WXSS change set, keeping template logic declarative and all derived fields in TypeScript.
5. Run targeted tests, repository gate and `git diff --check`.
6. Obtain a 375x812 mini-program screenshot after compile, compare it to the new Figma state, update documentation and create a path-limited commit.
