# C14 团队能力总览运行态视觉验收

## 目标

以在线 Figma `zZ6wKyOHKcO4UYXDd9jGwv` 的 C14 节点 `93:1106` 为唯一设计基准，在真实教练会话下对 `pages/coach/team-ability/index` 取得严格 `375×812` 模拟器截图。发现页面级差异时，只做 C14 本页最小修复；真实团队能力数据、不可用排名和导出边界不得被画板样例替代。

## 约束

- 每次视觉判断前重新读取在线 Figma 节点，并保留设计参考、运行态截图和对照图。
- 截图使用微信开发者工具屏幕像素通道，不用 Automator canvas 截图代替视觉证据。
- 不修改 API、角色守卫、团队能力 BFF、雷达数据源或其他页面；WXML 不调用数组方法。
- 当前工作区既有未提交改动属于他人在途，提交必须路径限定。

## 验收标准

- [x] 在线节点、真实路由、首屏和必要的滚动截图均有可追溯证据。
- [x] 顶栏、导出控件、上下文、团队雷达卡、综合分/趋势、维度统计、排名卡和教练 TabBar 的差异逐项分类为已修复、真实数据差异或系统壳层差异。
- [x] 若有代码差异，先补定向回归，再做最小修复并重新截图；若无代码差异，记录为运行态视觉通过，不制造无关改动。
- [x] C14 测试、小程序 typecheck、全仓 check 和 `git diff --check` 通过。
- [x] 仅提交 C14 源码/测试、C14 文档/规格和本任务归档记录。

## 范围外

- 不新增或伪造团队成员、排名、评估期、总分、趋势或导出成功响应。
- 不清理、回滚或覆盖当前其他未提交文件。
