# Technical design

The pages remain route-compatible and consume existing parent growth/metric normalizers. The page view model must remove prior fixed-size/synthetic visual facts instead of expanding contracts. Canvas code is out of scope; only page-owned inputs/visibility may change.

## Allowlist

- `apps/miniprogram-cq-talent/pages/parent/radar/index.{ts,wxml,wxss,test.mjs}`
- `apps/miniprogram-cq-talent/pages/parent/metric/index.{ts,wxml,wxss,test.mjs}`
- this task directory
