# Technical design

P9 remains page-owned and converts real user-entered fields to the existing create request. P9.1 fetches the request list for the real child and finds the returned request id before presenting its summary. Unsupported Figma samples are absent rather than synthesized.
