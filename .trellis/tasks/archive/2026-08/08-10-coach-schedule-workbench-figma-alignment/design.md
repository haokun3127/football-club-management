# Technical design

Each page uses a page-owned presenter and existing API request boundary. Unsupported Figma samples become unavailable states rather than fabricated business facts. C3 uses its existing persisted change-request endpoint only.
