# C13 implementation plan

1. Change the focused test first to reject the compressed header box model.
2. Apply only the C13 header box-model adjustment and retain the existing
   canvas, score, empty-state, request-token and BFF behavior.
3. Run the C13 suite, mini-program typecheck, root gate and diff check.
4. Record the Figma/readback/screenshot boundary and commit only this page and
   task documentation.
