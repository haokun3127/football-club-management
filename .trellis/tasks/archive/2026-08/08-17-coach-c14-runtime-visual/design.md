# C14 技术设计

## 数据边界

页面继续使用 `getCoachTeamAbilityOverview()` 和 `getCoachTeam()`：团队能力响应驱动雷达、综合分、趋势、维度统计和排名不可用文案；团队响应只驱动上下文。所有画板示例姓名、日期和分数均视为参考数据，不写入 view model。

## 视觉边界

先检查在线节点 `93:1106` 与当前截图。若发现安全区、卡片尺寸、重叠或固定底栏问题，只修改 `pages/coach/team-ability/` 的 WXML/WXSS/测试；保持已有 1040rpx 深色雷达卡和 radar/empty 互斥结构。雷达 canvas 只通过屏幕像素截图验收。

## 回滚

任何修复只限 C14 文件；若截图显示差异来自真实数据或微信系统壳层，则不改代码，仅在 C14 规格和进度中记录豁免原因。
