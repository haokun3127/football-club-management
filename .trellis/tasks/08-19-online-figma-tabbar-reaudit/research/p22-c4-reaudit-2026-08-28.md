# P2.2 / C4 online Figma re-audit — 2026-08-28

## P2.2 Other Activity Detail — `93:198`

- Live online Figma was read with `get_design_context` and `get_screenshot`; reference is `live-2026-08-28/p2-2-other-activity-online.png`.
- Runtime route `/pages/parent/event/index?id=event-cq-talent-family-assessment-day` was opened after the real coach-to-parent role switch. The page reached `ready` and the screenshot is strict `375×812`.
- Top navigation, status chip, hero/type chip, metadata row, information rows, participant card, coach card, footer note and four-item parent TabBar all correspond to the board. Runtime values are API-backed and intentionally differ from Figma's static sample (`俱乐部活动`/`待开始`/`青训体测与家长说明会`/地点待确认 versus `体测`/`已确认`/`青训体测日`/`室内训练场`).
- Disposition: **pass with live API data-state exemption**. No business-code or Figma change was warranted.

## C4 Attendance — `93:665`

- Live online Figma was read with `get_design_context` and `get_screenshot`; reference is `live-2026-08-28/c4-attendance-online.png`.
- Runtime route `/pages/coach/attendance/index?id=event-cq-talent-secure-test-1-trn-0818` was opened with the real coach session. The page reached `ready`; first and bottom viewport screenshots are `c4-attendance-runtime.png` and `c4-attendance-runtime-bottom.png`, both strict `375×812`.
- The soft-pink header, title/action alignment, dark session summary, quick actions, roster cards, state colors, count and fixed coach TabBar are structurally consistent with the board. The real activity title/metadata wraps to two lines and the real roster is eight players, so fewer rows are visible in the first viewport than the Figma's shorter static sample; the bottom viewport confirms the remaining roster and fixed TabBar are unobscured.
- Disposition: **pass with live API data-state exemption**. No business-code or Figma change was warranted.

## Verification

- Both runtime routes returned `state=ready`.
- Simulator console filter `error|exception|fail|undefined|route is not defined|wx:else` returned no matches.
- No code was changed in this P2.2/C4 batch; evidence and this record are path-limited additions.
