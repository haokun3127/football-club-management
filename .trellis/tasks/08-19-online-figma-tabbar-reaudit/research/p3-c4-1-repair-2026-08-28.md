# P3 / C4.1 live Figma repair evidence — 2026-08-28

## Evidence standard

The live design source was read from Figma file `zZ6wKyOHKcO4UYXDd9jGwv` before the repair comparison. Each runtime frame below was captured through WeChatIDE MCP at a strict `375×812` simulator viewport. The evidence distinguishes online design read, runtime capture, and visual comparison.

## P3 Reminder Center — node `93:222`

- Online Figma read: `research/live-2026-08-28/p3-reminder-center-figma-online.png`.
- Runtime capture before repair: `research/live-2026-08-28/p3-reminder-center-runtime.png`.
- Confirmed defect: the local top navigation rendered an extra `返回` text label and used a larger local back/title treatment than the current online board.
- Repair: `pages/parent/reminders/index.wxml` now keeps only the chevron icon; `index.wxss` uses a 48rpx icon slot and the live 18px visual title size; the focused test asserts the live icon-only contract.
- Runtime recapture: `research/live-2026-08-28/p3-reminder-center-runtime-repaired.png`, strict `375×812`.
- Comparison: the navigation shell, title alignment, reminder list structure, and parent TabBar agree with the online board. Reminder count/content remains real API data and is not treated as a design mismatch.
- Disposition: **repaired-and-recaptured**.

## C4.1 Attendance Success — node `93:696`

- Online Figma read: `research/live-2026-08-28/c4-1-attendance-success-figma-online.png`; the white outline check asset is retained as `c4-1-check-figma.svg`.
- Runtime capture before repair: `research/live-2026-08-28/c4-1-attendance-success-runtime-rerun.png`.
- Confirmed defect: the local success circle used a text `✓`, which did not match the current Figma white outline icon.
- Repair: `pages/coach/attendance-success/index.wxml` now uses `/assets/icons/c4-1-check.svg` with `mode="aspectFit"`; `index.wxss` sizes the asset to 40px; the focused test prevents regression to the text glyph.
- Runtime recapture: `research/live-2026-08-28/c4-1-attendance-success-runtime-repaired.png`, strict `375×812`.
- Comparison: the success circle, title/subtitle, summary rows, primary action, and coach TabBar agree with the online board. Attendance values are computed from the real workbench response and are not replaced with Figma sample numbers.
- Disposition: **repaired-and-recaptured**.

## Verification

- Focused regression: 2 test files, 9/9 tests passed.
- Full gate with isolated npm cache: typecheck passed for domain, mini-program, and API; domain 20/20, mini-program 408/408, API 115/115.
- `git diff --check`: passed.
- The first full-gate invocation was blocked before project execution by the machine-wide npm cache `EEXIST/EBADF` conflict; retrying with a repository-local isolated cache completed successfully.
