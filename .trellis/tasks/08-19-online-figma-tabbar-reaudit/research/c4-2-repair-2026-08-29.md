# C4.2 Attendance Correction live Figma repair — 2026-08-29

## Evidence

- Live Figma file: `zZ6wKyOHKcO4UYXDd9jGwv`, node `93:715`, read with `get_design_context` and `get_screenshot` before editing.
- Online reference: `research/live-2026-08-29/c4-2-attendance-correction-figma-online.png`, natural size `375×812`.
- Runtime route: `/pages/coach/attendance/index?id=event-cq-talent-secure-test-1-trn-0818&correction=1`, reached with the existing real coach session.
- Runtime before repair: `research/live-2026-08-29/c4-2-attendance-correction-runtime.png`, strict `375×812`.
- Runtime after repair: `research/live-2026-08-29/c4-2-attendance-correction-runtime-repaired.png`, strict `375×812`.
- Bottom viewport after repair: `research/live-2026-08-29/c4-2-attendance-correction-runtime-bottom.png`, strict `375×812`.

## Confirmed differences and repair

- The online board uses an 18px visual title in the shared top navigation. The route had passed `large-title`, which rendered the title at the larger local variant. The `large-title` attribute was removed.
- The online board uses the exported 24px white outline `alert-triangle` inside the 48px orange circle. The route used a hand-drawn CSS triangle and a text glyph. The exact asset from the live Figma response is now committed as `/assets/icons/c4-2-alert-triangle.svg` and rendered with `mode="aspectFit"` at `48rpx` inside the `96rpx` circle.
- The online board's explanation is `家长对出勤记录提出异议，请核实后重新提交`; the route now uses this complete copy.
- The online sample has two disputed students. The runtime returned eight real roster entries with mixed attendance states; this is a live API data-state difference, not fabricated into the sample state. The bottom viewport confirmed all real rows, the correction note and the fixed submit action remain reachable without TabBar overlap.

## Verification

- TDD regression: C4.2 focused test was intentionally red at `2 failed / 6 passed`, then green at `8/8` after the repair.
- WeChatIDE `compile_wxml` and `compile_wxss` both returned success.
- Runtime current-page check confirmed the correction route and simulator console filter `error|exception|fail|undefined|route is not defined|wx:else` returned no matches.
- Disposition: **repaired-and-recaptured**.
