# P2.1 / C3 online Figma re-audit — 2026-08-28

## Evidence standard

For both boards, the live file `zZ6wKyOHKcO4UYXDd9jGwv` was read with Figma MCP `get_design_context` and `get_screenshot` before visual judgement. Runtime frames were captured through WeChatIDE MCP at a strict `375×812` simulator viewport. Native status-bar, notch, WeChat capsule and home-indicator rendering remain platform exemptions.

## P2.1 Match Detail — `93:170`

- Online evidence: `live-2026-08-28/p2-1-match-detail-online.png` and sidecar. The current board requires a 24px back icon slot, title beginning at x=40, complete `比赛详情` title, red `邀请好友` action, dark match hero, attendance roster, match-event card, and the four-item parent TabBar.
- Runtime route: `/pages/parent/event/index?id=event-cq-talent-secure-test-1-scheduled-match`, reached after the real coach-to-parent role switch. Runtime state is real API data: scheduled match, real two-person roster, `0:0`, venue pending, and no recorded events. This intentionally differs from the Figma sample values and is recorded as a data-state exemption.
- A real completed match was also inspected and showed the API-backed `4:2` score/events; a different demo match returned the truthful parent-membership `403`. Neither was used to overwrite the scheduled-match sample state.
- First comparison found a real local defect: `.p2-nav__back` occupied `80rpx`, moving the title to x=56, and the native share `<button>` defaulted to a `184px` box, truncating the title to `比…`.
- Focused regression was red before the repair and green after it: `pages/parent/event/index.test.mjs` went from `1 failed / 4 passed` to `5 passed`. The repair is limited to `pages/parent/event/index.wxss`: back slot `48rpx`; share button `width:auto; min-width:0; margin:0`.
- Re-captured runtime evidence: `live-2026-08-28/p21-scheduled-match-runtime-repaired.png` and sidecar. Runtime element checks report title text `比赛详情`, share button width `56px`, route verified, and simulator console error filter empty.
- Disposition: **repaired-and-recaptured**. Parent TabBar geometry/state remains consistent with the live overlay; match content differences are real API state, not design defects.

## C3 Activity Change — `93:634`

- Online evidence: `live-2026-08-28/c3-activity-change-online.png` and sidecar. The board requires the soft-pink top navigation, left back/title group, red `保存`, activity card, reason chips, new time, new venue, notes, notification switch, and coach TabBar.
- Runtime route: `/pages/coach/event-change/index?id=event-cq-talent-secure-test-1-trn-0818`, reached after the real parent-to-coach role switch. The page was `ready`; activity title, real venue options, and affected-parent count came from the existing API.
- Fresh first-viewport and bottom-viewport frames are `live-2026-08-28/c3-activity-change-runtime-recaptured.png` and `live-2026-08-28/c3-activity-change-runtime-recaptured-bottom.png`. The bottom frame confirms the notification card and fixed coach TabBar remain visible without overlap.
- Figma's prefilled date, venue, sample team name and `20` parents are static design examples. The runtime preserves truthful empty new-value fields and the real affected-parent count; no mock data or API changes were introduced.
- Disposition: **pass with live API data-state exemption**. No business-code repair was warranted in this batch.

## Verification

- Focused P2.1 test: `5/5` passed.
- Mini-program TypeScript check: `tsc --noEmit` exited `0`.
- Runtime console filter for `error|exception|fail|undefined|route is not defined|wx:else`: no matches for the repaired P2.1 and fresh C3 route.
