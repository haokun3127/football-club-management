# 执行计划

1. 读取 domain/API/persistence/app-client specs，确认 event access 和 repository 模式。
2. 增加 domain 类型、阵型模板和坐标校验纯函数测试。
3. 增加 SQLite migration/repository/seed 和重启持久化测试。
4. 增加 app-client formations、GET/PUT board 路由、schema、OpenAPI、权限与幂等契约测试。
5. 增加小程序类型/API client/路由和绿色球场拖拽 PoC。
6. 接真实 roster、阵型、替补池、保存/重置/只读和比赛入口。
7. 更新 Figma C7 并做不同屏幕截图对比。
8. 运行全量检查、smoke、DevTools 拖拽/滚动/重进测试。
9. Trellis check、spec 更新、提交归档。
