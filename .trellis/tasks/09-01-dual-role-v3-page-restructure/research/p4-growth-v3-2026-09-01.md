# P4 Growth V3 · Figma and runtime comparison

## Sources

- Online Current Product V3 frame: `zZ6wKyOHKcO4UYXDd9jGwv / 1467:185`.
- Historical source frame (not changed): `zZ6wKyOHKcO4UYXDd9jGwv / 93:250`.
- Runtime route: `/pages/parent/growth/index`.

## Evidence

- Figma MCP frame screenshot: `%TEMP%\cq-talent-figma-v3-pages-20260901\p4-growth-v3-match-card-viewport-fixed.png`.
- WeChatIDE MCP normalized runtime screenshot: `%TEMP%\cq-talent-visual-evidence\wechatide-mcp-1788198699162-3351236472724.png` and its `.json` sidecar.
- The raw simulator capture was `563×1218`; the trusted project capture script validated aspect ratio and emitted the exact `375×812` PNG.

## Finding and correction

The runtime Growth home already rendered the parent-facing match-history affordance. The migrated historical P4 Figma frame did not, so the V3 frame added a `Match Record Card` after Milestones and before Training History. The P4 vertical content frame required auto-layout insertion at index 2 and `clipsContent=true`; assigning absolute y positions alone did not determine its order.

The historical P4 bottom navigation used the obsolete child/discover placement. Only the V3 copy now uses the verified current parent navigation order: 日程 / 成长 / 发现 / 我的孩子.

## Verdict

Figma and runtime have the same top navigation, player Hero, milestone group, match card, training-history entry and fixed parent TabBar structure. Real data values remain dynamic and are not design defects. No mini-program code change was needed in this page batch.
