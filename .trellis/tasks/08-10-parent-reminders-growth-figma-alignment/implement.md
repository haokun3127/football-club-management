# Execution

1. Add and observe RED tests for P3 state exclusivity/local read state and P4 true-data rendering.
2. Apply minimal page-local fixes; do not invent missing growth history.
3. Run focused/package/root checks and diff check, then commit only allowed paths.
