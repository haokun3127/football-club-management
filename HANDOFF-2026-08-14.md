# 交付交接 — 2026-08-14 教练端 C1-C3 复原验收 + 家长端收官

> 上一版交接：HANDOFF.md（2026-08-13）。本文档只写 2026-08-14 的增量与当前最新状态。

## 一、本次完成内容

### 家长端（全部 ✅ 收官）
- P1–P10 共 21 板全部通过 375×812 截图验收。
- 最后交付：P10 账号绑定家庭成员（family 接口 + 成员列表 + 添加 CTA + 微信行脱敏手机号），提交 `080b5d7` 已部署生产；`94ee8af` 按钮卡片边框样式。

### 教练端（28 板进行中，已完成 3 板）
| 板 | 页面 | 状态 | 关键提交 |
|---|---|---|---|
| C1 排期主页 | pages/coach/schedule | ✅ 验收通过 | `3cc73ad`（coachName+出席胶囊+weekStats+红点）、`8fbce45`（hero排版）、后续周日历/年份日期 fix |
| C2 活动工作台 | pages/coach/event | ✅ 验收通过（进行中态） | `40c4a15`（finish 接口+倒计时+出勤卡）、`de30c63`/`911b328`/`54960f8`/`23b98da`（时长口径/标签/时钟SVG） |
| C3 活动变更 | pages/coach/event-change | ✅ 验收通过 | `da22dc5`（新时间/新场地常显+场地选择器+通知家长开关+0011 迁移 notify_parents 列）、`2c71faf`（teamName 兜底） |

### 服务端新增（均已部署生产，health 200）
1. `POST /coach/events/:eventId/finish` — 结束训练（scheduled→completed，重复 409）。
2. coach/home 响应新增 `coachName`、`summary.attendance{confirmed,total}`、`weekStats{sessions,hours,attendanceRate}`（周=含 from 的周一至周日）。
3. change-requests 请求体新增 `notifyParents`（迁移 `0011_event_change_notify.sql`，列 `notify_parents` 默认 1）。

### 生产数据（直写库，已备份）
- 今日新增 2 场活动（19:00 体能综合训练、17:30 友谊赛 match）+ 10:30 训练事件窗口改为进行中区间、participant 陈小宇置 present（C2 进行中态验收用）。
- 备份在容器卷内 `/var/lib/cq-talent/backup-*.sqlite`（见 tmp/prod-verify/prod-c1-backup.py 输出）。

## 二、设计偏差记录（有意保留，非缺陷）
- C1/C2 数值（日期、出席 1/1、100%、8.5h 等）= 真实数据差异，按纪律豁免。
- C2「训练内容进度卡」（热身/技术/对抗分项进度）：数据模型无 per-item 进度，保留现有「训练内容」数据卡。
- C2 底部三按钮（记录比分/课后评价/保存进度）：保留数据驱动的 action-grid（真实能力超集）。
- C2「学分 0点」标签：无学分数据，不虚构。
- C3 场地选择器：venues 为空时回退文本输入（wx:else）。

## 三、关键 SOP（2026-08-14 更新）
1. **教练会话**：`uv run --with paramiko python tmp/prod-verify/prod-plant-session-coach.py`（服务端种 coach 会话）→ `PLANT_ROLE=coach PLANT_TOKEN=*** PLANT_EXPIRES='2026-09-13T13:29:03.429Z' MP_AUTO_PORT=9432 node scripts/devtools/mp-plant-session.cjs`。当前 token `wx-session-86c534eb-4e22-485f-b195-686d88145449`，有效期 2026-09-13。
2. **截图通道**：automator screenshot 在本机持续超时（实例健康也一样）。有效通道 = `tmp/prod-verify/screen-shot.py`（win32 PrintWindow 整窗）→ `crop-phone.py`（裁剪 1395,90,1778,895 → 375×812）→ `sidebyside.py`。**前置：Edge 等遮挡窗口先最小化**（ShowWindow 6）。
3. **IDE 文件监听不可靠**：提交后模拟器可能跑旧 bundle，reLaunch 不解决 → 请用户点一次「编译」。现象=新功能不出现但路由正常。
4. **白屏死态**：整窗纯白时清 GPU 缓存/cli 重启均无效，唯一可靠恢复=用户手动完全退出 IDE（含托盘）重开项目。
5. **403 排查口诀**：先查服务端会话 role（prod-plant-session*.py）与请求角色是否匹配。
6. **「进行中」是推导态不是存储态**：CalendarEventStatus 只有 scheduled/cancelled/completed；in_progress = scheduled && now∈[startsAt,endsAt]（C2 倒计时/结束训练据此）。

## 四、剩余工作（下一会话）
- C4 点名（pages/coach/attendance，c4-attendance.png + C4.1 成功态 + C4.2 订正态）→ C5–C16.4 共 25 板，按 docs/design/reference/figma/README.md 对照表逐页复原验收。
- 画板↔路由权威对照：`docs/design/reference/figma/README.md`（注意：离线 PNG 编号与旧 frame-map 文档不同，以 README 为准）。
- 已知小项：C3 通知人数取自 roster（当前活动 1 人）；C2 倒计时=已进行时长（设计稿语义）。
- master 待 fast-forward 到 dev（遗留）。

## 五、验证基线
- 最近门禁：`npx --yes pnpm@10.33.0 run check` → exit=0（domain 19 / miniprogram 318 / api 104 全 passed）。
- 生产：https://cqtc.pomi.tech/health 200（容器 cq-talent-api，compose 服务名 api，端口 3000）。
