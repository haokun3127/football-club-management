<view wx:if="{{state !== 'ready' && state !== 'idle'}}" class="status status-{{state}}">
  <view class="status-title">{{title}}</view>
  <view class="status-message">{{message}}</view>
  <button wx:if="{{actionText}}" class="status-action" bindtap="handleAction">{{actionText}}</button>
</view>
