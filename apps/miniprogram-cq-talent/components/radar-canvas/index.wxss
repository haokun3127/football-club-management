.radar {
  position: relative;
  width: 100%;
  height: 520rpx;
}

.radar-canvas {
  width: 100%;
  height: 520rpx;
}

.radar-empty {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #60646f;
  background: transparent;
}
