<view class="content-page">
  <view class="page-nav page-nav--plain" style="padding-top:{{navInset}}px;padding-right:{{menuInset}}px">
    <view class="page-nav__title">内容中心</view>
    <view class="page-nav__action page-nav__action--disabled" aria-disabled="true"><image class="page-nav__action-icon" src="/assets/icons/search.svg" mode="aspectFit" /></view>
  </view>
  <view class="content-page__body">
    <status-view
      state="{{state}}"
      title="{{state === 'loading' ? '加载内容中心' : state === 'empty' ? '暂无内容' : '读取失败'}}"
      message="{{message}}"
      action-text="{{state === 'error' ? '重试' : ''}}"
      bindaction="loadArticles"
    />

    <block wx:if="{{state === 'ready'}}">
      <view class="section-title">分类导航</view>
      <scroll-view scroll-x class="pills" enhanced show-scrollbar="{{false}}">
        <view class="pills__inner">
          <view
            wx:for="{{categories}}"
            wx:key="value"
            class="pill {{activeCategory === item.value ? 'pill--active' : ''}}"
            data-value="{{item.value}}"
            bindtap="selectCategory"
          >{{item.label}}</view>
        </view>
      </scroll-view>


      <view class="section-title">快速入口</view>
      <view class="quick-grid">
        <view
          wx:for="{{quickLinks}}"
          wx:key="label"
          class="quick-card"
          data-category="{{item.category}}"
          bindtap="openQuickLink"
        >
          <view class="quick-card__icon" style="background: {{item.color}}19; color: {{item.color}}">{{item.icon}}</view>
          <view class="quick-card__label">{{item.label}}</view>
        </view>
      </view>


      <view class="section-title">最近文章</view>
      <view class="article-list">
        <view wx:if="{{hasVisibleArticles}}">
          <view wx:for="{{visibleArticles}}" wx:key="id" class="article-card">
            <view class="article-card__accent" style="background: {{item.accent}}"></view>
            <view class="article-card__content">
              <view class="article-card__title">{{item.title}}</view>
              <view class="article-card__subtitle">{{item.subtitle}}</view>
            </view>
          </view>
        </view>
        <view wx:else class="article-empty">{{emptyMessage}}</view>
      </view>
    </block>
  </view>
  <role-tabbar role="parent" active="child" />
</view>
